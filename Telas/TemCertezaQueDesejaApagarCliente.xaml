<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             x:Class="diagrama.ConfirmacaoApagarClientePage">

    <ContentPage.Content>
        <VerticalStackLayout Spacing="20"
                             Padding="20">
            <Label Text="Tem certeza que deseja apagar o cliente?"
                   FontSize="Title"
                   HorizontalTextAlignment="Center"/>

            <Grid ColumnDefinitions="*,*">
                <Button Text="Não"
                        Grid.Column="0"
                        BackgroundColor="Red"
                        TextColor="White"
                        Clicked="NaoButton_Clicked"/>
                <Button Text="Sim"
                        Grid.Column="1"
                        BackgroundColor="Green"
                        TextColor="White"
                        Clicked="SimButton_Clicked"/>
            </Grid>
        </VerticalStackLayout>
    </ContentPage.Content>
</ContentPage>
